<%= provide(:title, @user.nickname) %>
<div class="mt-5">
  <h1><%= @user.nickname %></h1>
  <% if @user.posts.any? %>
    <h3>Posts (<%= @user.posts.count %>)</h3>
    <ol class="posts">
      <%= render @posts %>
    </ol>
  <% end%>
</div>