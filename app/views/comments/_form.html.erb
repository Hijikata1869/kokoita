<div class="comment-form mt-4 mb-2">
  <%= form_for([@post, @comment]) do |f| %>
    <%= f.text_area :content, class: 'form-control', :placeholder => 'コメントを入力' %>
    <div class="text-right">
      <%= f.submit 'コメントする', class: 'btn btn-sm btn-success mt-2' %>
    </div>
  <% end %>
</div>
